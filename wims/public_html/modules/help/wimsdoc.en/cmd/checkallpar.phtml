!! +exec+ +adm+
tit:checkallpar
type:$type_adm
alias:
syntax:!checkallpar ``string''
mean: check if parentheses (all types) are balanced in ``string'' and return ``yes''\
  if it is the case and ``no'' if not.
