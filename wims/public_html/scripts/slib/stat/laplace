!if $wims_read_parm!=slib_header
  !goto proc
!endif

slib_parms=1\
,m

slib_author=Sophie, Lemaire; Bernadette, Perrin-Riou


slib_example= 5\
[2,3]
slib_require=
!exit

:proc
slib_out=
!distribute item $wims_read_parm into slib_data
slib_data=!declosing $slib_data
slib_cnt=!itemcnt $slib_data
!if $slib_cnt=1
  slib_data=1,$slib_data
!endif
!readproc slib/stat/commun stat
!for slib_x=1 to $(slib_data[1])
  !reset slib_line
  slib_pp=!random 0,1 repeat $(slib_data[2])
  !for slib_y=1 to $(slib_data[2])
    slib_p=$(slib_pp[$slib_y])
    !if $slib_p<0.5
      slib_line=!append item $[log(2*$slib_p)] to $slib_line
    !else
      slib_line=!append item $[-log(2*(1-$slib_p))] to $slib_line
    !endif
  !next
  slib_out=!append line $slib_line to $slib_out
!next
slib_out=!trim $slib_out
slib_out=!replace internal $\
$ by ; in $slib_out
slib_out=!words2items $slib_out
