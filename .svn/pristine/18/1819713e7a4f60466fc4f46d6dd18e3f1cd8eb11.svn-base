index: tous les polyedres
index1:interrogeable par record
index2: polyedre, s,f,a,doublon eventuel (les doublons sont enlevés ou à enlever).

index.lang : nom dans la langue lang (utilisable avec lookup)

indexdual: polyedre:son dual (utilisable avec lookup), non terminé
