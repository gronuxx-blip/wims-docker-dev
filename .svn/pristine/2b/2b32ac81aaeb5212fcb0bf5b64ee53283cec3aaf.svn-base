!! Este archivo contiene los mensajes de error. Se procesará
!! cuando la variable `error' no esté vacía.

<span class="wims_warning">$wims_name_Error.</span>

!if bad_data iswordof $error
  $name_msg_bad_data
  !exit
!endif

!msg $error
